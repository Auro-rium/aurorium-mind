# Shared UI components

No reusable UI components currently exist. The only interactive elements are
implemented inline in `app/page.tsx`; the redesign should extract a chat shell,
message card, composer, and status indicator.

