# Compact token summary

There is no stylesheet, token system, CSS framework, custom font, spacing scale,
or component library. Current appearance is browser-default HTML. The redesign
must introduce a deliberate dark, editorial AI-workspace system while retaining
high contrast, accessible focus states, and responsive layout behavior.

# Raw source

No `globals.css`, Tailwind configuration, or theme provider exists.

