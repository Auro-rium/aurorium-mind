# Key page dependency trees

## / (Chat)

Entry: `app/page.tsx`

Dependencies:

- `app/page.tsx`
- `app/layout.tsx`

The rendered page is an inline client component with a heading, short subtitle,
textarea, send button, and conditional `pre` answer block.

